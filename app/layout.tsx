export const metadata = {
  title: "GW2 | Página Inicial",
  description: "Bienvenido al mundo de los gatitos nwn.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
